# =============================================================================
# FreeCoding — ChronoCoding AI 编程智驾框架 .gitignore
# =============================================================================
#
# 策略说明：
# - .qoder/ 作为 Qoder 默认最佳实践实例提交到仓库，用户 clone 即用
# - .trae/ .cursor/ .windsurf/ 由 install 脚本生成，不提交
# - 仅排除含密钥的本地配置和临时运行产物
# =============================================================================

# ─── 密钥/本地配置（绝不提交）──────────────────────────────────────────────────
settings.local.json
*.local.json
*.local.config
.env
.env.*
!.env.example
env.json

# .qoder/ 中的密钥文件
.qoder/settings.local.json
.qoder/.env
.qoder/.env.*

# 其他 IDE 的密钥文件
.trae/settings.local.json
.trae/.env
.cursor/settings.local.json
.cursor/.env
.windsurf/settings.local.json
.windsurf/.env

# ─── 其他 IDE 部署目录（由 install 脚本生成）──────────────────────────────────
.trae/
.cursor/
.windsurf/

# ─── 运行时临时产物（不提交）──────────────────────────────────────────────────
# 状态追踪
*-state.json
*-state.json.bak
hang-state.json
hang-state.json.bak
hammer-state.json

# 评审产物（运行 /夯 后生成的临时文件）
red-*.md
blue-*.md
green-*.md
*-alignment.md
*-review.md
*-result.md
*-tdd-cycle-*.md
*-red-report.md

# 门控/覆盖率输出
gate-*.json
gate-*.md
coverage/
.nyc_output/

# 临时输出
tmp-*.md
temp-*.md
output-*.json

# ─── 记忆/会话文件────────────────────────────────────────────────────────────
memory/session/*.json
memory/user/*.json
memory/project.json.bak

# ─── 监控/日志文件────────────────────────────────────────────────────────────
monitor/*.log
monitor/*.tmp
*.log
*.tmp

# ─── 脚本执行产物────────────────────────────────────────────────────────────
scripts/output/
scripts/results/
scripts/.cache/

# ─── Node.js 运行时产物────────────────────────────────────────────────────────
node_modules/
*.node
.npm/
.yarn/

# ─── 编辑器/IDE 临时文件──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# ─── 系统文件─────────────────────────────────────────────────────────────────
.gitignore.local
.gitignore.sensitive

# ─── 设计文档/迁移方案（内部文档，不提交）─────────────────────────────────────
docs/TDD融入夯技能升级方案.md
*通用适配方案*.md
*实施计划*.md